# Maintainer: Timofei Zhakov

pkgname=xdigest
epoch=1
pkgver=1.0.0
pkgrel=1
pkgdesc='Fast digest algorithms'
arch=('x86_64')
url='https://github.com/rinrab/xdigest'
license=('Apache 2.0')
makedepends=('git' 'make' 'ninja')
depends=('glibc')
provides=('libxdigest.so')
source=("git+https://github.com/rinrab/xdigest")
sha256sums=('SKIP')

build() {
  cd ${pkgname}
  make -I $pkgname
}

check() {
  cd ${pkgname}
  make -I $pkgname test
}

package() {
  cd ${pkgname}
  make -I $pkgname install prefix=${pkgdir}/usr
}

# vim:set ts=2 sw=2 et:
